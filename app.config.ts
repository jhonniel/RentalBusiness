export default defineAppConfig({
  ui: {
    colors: {
      primary: 'lumen',
      neutral: 'stone',
    },
    button: {
      defaultVariants: {
        color: 'primary',
      },
    },
    container: {
      base: 'w-full max-w-6xl mx-auto px-4 sm:px-6 lg:px-8',
    },
  },
})
